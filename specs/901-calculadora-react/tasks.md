# Tasks — 901-calculadora-react

- [x] **T001** — Scaffold del proyecto React+TS con Vite (`pnpm create
  vite@latest app -- --template react-ts`), instalar dependencias y dejar
  `pnpm dev` corriendo sin errores. US: —

- [x] **T002** — El usuario puede ver la calculadora en pantalla con el
  grid completo de botones (dígitos, `.`, operadores, `=`, `C`) y una
  pantalla mostrando `0` al cargar. US-1

- [x] **T003** — El usuario puede construir una operación completa
  clickeando dígitos y un operador, viendo el número/operación reflejados
  en pantalla en tiempo real, y obtener el resultado correcto al clickear
  `=` (incluyendo decimales). US-1, US-2

- [x] **T004** — El usuario que intenta dividir por cero ve un estado de
  error en pantalla en vez de un crash o un valor incorrecto, y puede
  seguir operando después. US-3

- [x] **T005** — El usuario puede resetear la calculadora a `0` en
  cualquier momento clickeando `C`, sin importar el estado previo
  (parcial, resultado o error). US-4

- [x] **T006** — Revisión final: sin `console.log` ni errores en consola,
  sin dependencias fuera de React/Vite, sin código muerto. US: —
